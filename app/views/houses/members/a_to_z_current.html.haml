#content.section--primary
  .container
    %h1= @house.name
    = render partial: 'pugin/components/navigation-letter', locals: { route_symbol: :house_members_current_a_z_letter_path, route_args: [@house_id], all_path: @all_path}
