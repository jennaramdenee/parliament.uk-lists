#content.section--primary
  .container
    %h1= @party.name
    = render partial: 'pugin/components/navigation-letter', locals: { route_symbol: :party_members_a_z_letter_path, route_args: [@party_id], all_path: @all_path }
